<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical"
    tools:context="mx.lania.registrogastos.historial">


    <DatePicker
        android:id="@+id/fecha"  android:visibility="invisible"
        android:layout_width="wrap_content"
        android:layout_height="1dp"
        android:calendarViewShown="false"

        android:datePickerMode="spinner" />
    <Button

        android:id="@+id/bt_agregar"
        style="?android:attr/buttonStyleSmall"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:onClick="agregar"
        android:text="Agregar"
        />


    <ListView
        android:layout_width="fill_parent"
        android:layout_height="300dp"
        android:id="@+id/gbhistorial"
        android:longClickable="true"
        android:clickable="true"

        tools:listitem="@android:layout/list_content" >


    </ListView>

    <ScrollView android:isScrollContainer="true"
        android:id="@+id/scrollView"
        android:layout_width="fill_parent"
        android:layout_height="200dp">


    </ScrollView>


</LinearLayout>
